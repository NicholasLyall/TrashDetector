---
phase: 02-hero-impact-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/mock-data.ts
  - apps/web/app/globals.css
  - apps/web/components/dashboard/hero-illustration.tsx
  - apps/web/components/dashboard/hero-impact-section.tsx
  - apps/web/app/page.tsx
autonomous: false
requirements: [DASH-02, HERO-01, HERO-02, HERO-03, HERO-04, HERO-06, HERO-07, VISL-01, VISL-03, VISL-04]

must_haves:
  truths:
    - "Dashboard homepage shows a hero impact section as the first visible element"
    - "Hero displays motivational message with today's waste diverted kg in eco-green"
    - "Hero shows 3 stat cards: Recycled %, CO2 Saved kg, Total Items Sorted"
    - "Hero has an illustrated SVG background with sky, clouds, and floating leaves (no globe)"
    - "Leaves animate with a gentle floating motion via CSS keyframes"
    - "Stat cards stack vertically on mobile and display in a 3-column grid on desktop"
    - "All text uses the Geist font with Display/Heading/Body size hierarchy"
    - "Hero card uses rounded corners, subtle shadow, and eco-gradient background"
  artifacts:
    - path: "apps/web/lib/mock-data.ts"
      provides: "Hardcoded hero mock data object"
      contains: "heroMockData"
    - path: "apps/web/app/globals.css"
      provides: "CSS keyframes for leaf float animation"
      contains: "@keyframes float"
    - path: "apps/web/components/dashboard/hero-illustration.tsx"
      provides: "SVG background with clouds and animated leaves"
      exports: ["HeroIllustration"]
    - path: "apps/web/components/dashboard/hero-impact-section.tsx"
      provides: "Complete hero section with motivational message and stat cards"
      exports: ["HeroImpactSection"]
    - path: "apps/web/app/page.tsx"
      provides: "Dashboard page rendering HeroImpactSection as first child"
      contains: "HeroImpactSection"
  key_links:
    - from: "apps/web/components/dashboard/hero-impact-section.tsx"
      to: "apps/web/lib/mock-data.ts"
      via: "import heroMockData"
      pattern: "import.*heroMockData.*from.*mock-data"
    - from: "apps/web/components/dashboard/hero-impact-section.tsx"
      to: "apps/web/components/dashboard/hero-illustration.tsx"
      via: "import HeroIllustration"
      pattern: "import.*HeroIllustration.*from.*hero-illustration"
    - from: "apps/web/app/page.tsx"
      to: "apps/web/components/dashboard/hero-impact-section.tsx"
      via: "import HeroImpactSection"
      pattern: "import.*HeroImpactSection.*from.*hero-impact-section"
    - from: "apps/web/app/globals.css"
      to: "apps/web/components/dashboard/hero-illustration.tsx"
      via: "CSS @keyframes float consumed by animate-float class"
      pattern: "@keyframes float"
---

<objective>
Build the Hero Impact Section -- the first and most prominent element on the dashboard homepage. It shows an inspiring motivational message ("Great job! You've diverted X kg of waste today.") over an illustrated eco-themed SVG background (sky, clouds, floating leaves) with 3 stat cards (Recycled %, CO2 Saved, Total Items Sorted) using hardcoded mock data.

Purpose: The hero section establishes the emotional impact of the product immediately -- judges and users see environmental metrics front and center, making the system feel like a real climate-tech SaaS product.

Output: HeroImpactSection component rendered as the first child on the dashboard page, with mock data, SVG illustration, floating leaf animations, 3 stat cards, responsive layout, dark mode support, and accessibility compliance.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-impact-section/02-CONTEXT.md
@.planning/phases/02-hero-impact-section/02-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/web/components/ui/card.tsx:
```typescript
function Card({ className, size = "default", ...props }: React.ComponentProps<"div"> & { size?: "default" | "sm" })
function CardHeader({ className, ...props }: React.ComponentProps<"div">)
function CardTitle({ className, ...props }: React.ComponentProps<"div">)
function CardContent({ className, ...props }: React.ComponentProps<"div">)
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent }
```

From apps/web/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From apps/web/components/layout/app-shell.tsx:
```typescript
// Main content area has: className="flex-1 overflow-y-auto p-6"
// style={{ backgroundColor: "hsl(var(--eco-bg))" }}
// Dashboard page children are rendered inside this <main> element
```

From apps/web/app/globals.css (eco palette):
```css
--eco-green: 142 71% 45%;
--eco-teal: 174 62% 47%;
--eco-blue: 199 89% 48%;
--eco-bg: 200 40% 94%;
--eco-card: 0 0% 100%;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock data, CSS animations, and SVG illustration component</name>
  <files>apps/web/lib/mock-data.ts, apps/web/app/globals.css, apps/web/components/dashboard/hero-illustration.tsx</files>
  <read_first>
    - apps/web/app/globals.css (current CSS variables and structure -- MUST preserve all existing content)
    - apps/web/lib/utils.ts (cn utility signature)
    - .planning/phases/02-hero-impact-section/02-UI-SPEC.md (full visual spec -- authoritative source)
  </read_first>
  <action>
**1. Create `apps/web/lib/mock-data.ts`** (per D-13):

```typescript
export const heroMockData = {
  wasteDivertedKg: 12.4,
  recycledPercent: 68,
  co2SavedKg: 21,
  totalItemsSorted: 241,
} as const;
```

Also export a `formatNumber` utility for future comma-separated thousands:

```typescript
export function formatNumber(value: number): string {
  return new Intl.NumberFormat("en-US").format(value);
}
```

**2. Append CSS keyframes to `apps/web/app/globals.css`** (AFTER the existing `@layer base` block -- do NOT modify any existing content):

Add a new block at the end of the file:

```css
/* Hero impact section animations */
@keyframes float {
  0%, 100% {
    transform: translateY(-6px);
  }
  50% {
    transform: translateY(6px);
  }
}

@keyframes cloud-drift {
  0%, 100% {
    transform: translateX(-4px);
  }
  50% {
    transform: translateX(4px);
  }
}

@layer utilities {
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
  .animate-cloud-drift {
    animation: cloud-drift 10s ease-in-out infinite;
  }
}

/* Respect reduced motion preference */
@media (prefers-reduced-motion: reduce) {
  .animate-float,
  .animate-cloud-drift {
    animation: none;
  }
}
```

**3. Create `apps/web/components/dashboard/hero-illustration.tsx`**:

Create the `components/dashboard/` directory if it does not exist.

Build an SVG illustration component with:
- Export: `export function HeroIllustration()`
- Wrapper: `<div className="absolute inset-0 z-0 overflow-hidden" aria-hidden="true" role="presentation">`
- Contains inline SVG elements (NOT an `<svg>` viewBox wrapper -- individual positioned SVG elements):

**Clouds** (2-3 cloud shapes):
- Cloud 1: positioned `top-[15%] left-[10%]`, white fill, opacity 0.7, `animate-cloud-drift` class, width ~120px
- Cloud 2: positioned `top-[25%] right-[15%]`, white fill, opacity 0.6, `animate-cloud-drift` with `animation-delay: 3s` via inline style, width ~90px
- Cloud 3: positioned `bottom-[35%] left-[55%]`, white fill, opacity 0.5, `animate-cloud-drift` with `animation-delay: 6s`, width ~100px
- Each cloud is an SVG with a simple cloud path (ellipses/rounded shapes)

**Leaves** (5 leaf shapes scattered at edges):
- Each leaf is an SVG with a simple leaf path (teardrop/pointed oval with center vein line)
- Fills: `hsl(142 71% 45%)` (eco-green) at varying opacities
- Leaf 1: `top-[10%] right-[8%]`, opacity 0.5, `animate-float`, `style={{ animationDuration: '3s', animationDelay: '0s' }}`
- Leaf 2: `top-[60%] left-[5%]`, opacity 0.4, `animate-float`, `style={{ animationDuration: '4s', animationDelay: '0.7s' }}`
- Leaf 3: `bottom-[15%] right-[20%]`, opacity 0.6, `animate-float`, `style={{ animationDuration: '3.5s', animationDelay: '1.2s' }}`
- Leaf 4: `top-[35%] left-[85%]`, opacity 0.3, `animate-float`, `style={{ animationDuration: '5s', animationDelay: '0.3s' }}`
- Leaf 5: `bottom-[25%] left-[15%]`, opacity 0.7, `animate-float`, `style={{ animationDuration: '4.2s', animationDelay: '1.8s' }}`, add `hidden sm:block` to hide on very small screens (per UI-SPEC responsive rules)
- Each leaf SVG: width 28-40px, height 28-40px, rotated at varying angles via `transform: rotate(Xdeg)` inline style

Each cloud and leaf is a separate absolutely-positioned `<svg>` element inside the wrapper div. Use `className="absolute"` plus Tailwind position utilities on each.

The component should be a client component (`"use client"`) since it uses inline styles for animation delays.

**Dark mode adjustments on the illustration:**
- Clouds: in dark mode reduce opacity further. Use `className="opacity-70 dark:opacity-15"` (etc.) on each cloud SVG.
- Leaves: in dark mode slightly brighter. Use `className="opacity-50 dark:opacity-40"` (etc.) -- the dark mode opacities can be slightly adjusted per leaf.
  </action>
  <verify>
    <automated>cd /Users/bzcruz/Projects/trash-detection-dashboard/apps/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/mock-data.ts contains `export const heroMockData`
    - apps/web/lib/mock-data.ts contains `wasteDivertedKg: 12.4`
    - apps/web/lib/mock-data.ts contains `recycledPercent: 68`
    - apps/web/lib/mock-data.ts contains `co2SavedKg: 21`
    - apps/web/lib/mock-data.ts contains `totalItemsSorted: 241`
    - apps/web/lib/mock-data.ts contains `export function formatNumber`
    - apps/web/lib/mock-data.ts contains `Intl.NumberFormat`
    - apps/web/app/globals.css contains `@keyframes float`
    - apps/web/app/globals.css contains `translateY(-6px)`
    - apps/web/app/globals.css contains `translateY(6px)`
    - apps/web/app/globals.css contains `@keyframes cloud-drift`
    - apps/web/app/globals.css contains `animate-float`
    - apps/web/app/globals.css contains `prefers-reduced-motion`
    - apps/web/app/globals.css still contains `--eco-green: 142 71% 45%` (existing content preserved)
    - apps/web/components/dashboard/hero-illustration.tsx contains `export function HeroIllustration`
    - apps/web/components/dashboard/hero-illustration.tsx contains `aria-hidden="true"`
    - apps/web/components/dashboard/hero-illustration.tsx contains `role="presentation"`
    - apps/web/components/dashboard/hero-illustration.tsx contains `animate-float`
    - apps/web/components/dashboard/hero-illustration.tsx contains `animate-cloud-drift`
    - apps/web/components/dashboard/hero-illustration.tsx contains `animationDelay`
    - apps/web/components/dashboard/hero-illustration.tsx contains `dark:opacity`
    - TypeScript compilation (`npx tsc --noEmit`) exits with 0
  </acceptance_criteria>
  <done>
    - mock-data.ts exports heroMockData with all 4 metric values and a formatNumber utility
    - globals.css has float and cloud-drift keyframes with prefers-reduced-motion media query
    - hero-illustration.tsx renders 2-3 clouds and 5 leaves with staggered CSS animations and dark mode opacity adjustments
    - All files pass TypeScript compilation
  </done>
</task>

<task type="auto">
  <name>Task 2: Build HeroImpactSection component and wire into dashboard page</name>
  <files>apps/web/components/dashboard/hero-impact-section.tsx, apps/web/app/page.tsx</files>
  <read_first>
    - apps/web/components/dashboard/hero-illustration.tsx (just created in Task 1 -- get the export name)
    - apps/web/lib/mock-data.ts (just created in Task 1 -- get the export names and values)
    - apps/web/components/ui/card.tsx (Card component API -- className prop, size prop)
    - apps/web/app/page.tsx (current dashboard page -- will be replaced)
    - apps/web/app/globals.css (eco palette variable names for gradient values)
    - .planning/phases/02-hero-impact-section/02-UI-SPEC.md (full visual spec -- authoritative source for all sizing, spacing, colors)
  </read_first>
  <action>
**1. Create `apps/web/components/dashboard/hero-impact-section.tsx`**:

This is the main hero section feature component. It must be a client component (`"use client"`) since it imports components that use client-side features.

**Imports:**
```typescript
import { Card } from "@/components/ui/card";
import { cn } from "@/lib/utils";
import { heroMockData, formatNumber } from "@/lib/mock-data";
import { HeroIllustration } from "@/components/dashboard/hero-illustration";
import { Recycle, Leaf, Package } from "lucide-react";
```

**Component structure** (per D-01, D-02, D-10, D-12):

```
<section> wrapper
  <Card> with relative positioning, gradient bg, overflow-hidden, min-h-[280px], rounded-xl, shadow-sm, ring-1 ring-foreground/5
    <HeroIllustration /> (absolute positioned background layer)
    <div> content wrapper with z-10, flex col, items-center, p-6 pt-12, md padding
      <p> Motivational message (per D-02, D-11):
        "Great job! You've diverted " + <span class eco-green>{value} kg</span> + " of waste today."
        - Classes: text-[28px] leading-[1.2] font-bold text-center max-w-lg
        - The "{value} kg" span: text-[hsl(var(--eco-green))] dark:text-[hsl(142_71%_55%)]
        - Responsive: text-[22px] on < md, text-[20px] on < sm (use Tailwind: `text-[20px] sm:text-[22px] md:text-[28px]`)
      </p>
      <div> stat cards grid (per D-01):
        - Classes: grid grid-cols-1 md:grid-cols-3 gap-4 w-full max-w-2xl mt-8
        - Contains 3 StatCard instances
      </div>
    </div>
  </Card>
</section>
```

**Hero Card gradient background** (per UI-SPEC Color section):
- Light mode: `background: linear-gradient(to bottom, hsl(199 89% 90%), hsl(174 62% 88%), hsl(142 71% 90%))`
- Dark mode: `background: linear-gradient(to bottom, hsl(199 89% 15%), hsl(174 62% 12%), hsl(142 71% 15%))`
- Apply via a custom CSS class. Add `className="hero-gradient"` on the Card.
- Append to globals.css (if not already added in Task 1):

```css
.hero-gradient {
  background: linear-gradient(to bottom, hsl(199 89% 90%), hsl(174 62% 88%), hsl(142 71% 90%));
}
.dark .hero-gradient {
  background: linear-gradient(to bottom, hsl(199 89% 15%), hsl(174 62% 12%), hsl(142 71% 15%));
}
```

**StatCard inline sub-component** (per D-04, D-05, UI-SPEC StatCard Data table):

Define a `StatCard` component inside the same file (not exported separately, per UI-SPEC "can be inline"):

```typescript
interface StatCardProps {
  readonly icon: React.ElementType;
  readonly iconBgColor: string;
  readonly iconColor: string;
  readonly value: string;
  readonly label: string;
}

function StatCard({ icon: Icon, iconBgColor, iconColor, value, label }: StatCardProps) {
  return (
    <Card className="bg-card shadow-sm">
      <div className="flex items-center gap-3 p-4">
        <div
          className="flex h-10 w-10 items-center justify-center rounded-lg"
          style={{ backgroundColor: iconBgColor }}
        >
          <Icon className="h-6 w-6" style={{ color: iconColor }} aria-hidden="true" />
        </div>
        <div className="flex flex-col">
          <span className="text-[20px] font-bold leading-[1.3]">{value}</span>
          <span className="text-[14px] text-muted-foreground">{label}</span>
        </div>
      </div>
    </Card>
  );
}
```

**StatCard instances** (per UI-SPEC StatCard Data table and D-04, D-05):

Card 1 - Recycled:
- icon: `Recycle`
- iconBgColor: `"hsl(142 71% 45% / 0.1)"` (eco-green 10%)
- iconColor: `"hsl(142 71% 45%)"` (eco-green)
- value: `formatNumber(heroMockData.recycledPercent) + "%"` -> "68%"
- label: "Recycled"

Card 2 - CO2 Saved:
- icon: `Leaf`
- iconBgColor: `"hsl(174 62% 47% / 0.1)"` (eco-teal 10%)
- iconColor: `"hsl(174 62% 47%)"` (eco-teal)
- value: `formatNumber(heroMockData.co2SavedKg) + " kg"` -> "21 kg"
- label: "CO2 Saved"

Card 3 - Total Items Sorted:
- icon: `Package`
- iconBgColor: `"hsl(199 89% 48% / 0.1)"` (eco-blue 10%)
- iconColor: `"hsl(199 89% 48%)"` (eco-blue)
- value: `formatNumber(heroMockData.totalItemsSorted)` -> "241"
- label: "Total Items Sorted"

**Responsive behavior** (per UI-SPEC Responsive Behavior):
- `>= md` (768px+): grid-cols-3 stat cards, Display text 28px, card padding p-6 pt-12
- `< md`: grid-cols-1 stat cards, Display text 22px, card padding p-4 pt-8
- `< sm` (375px): motivational message 20px

**Accessibility** (per UI-SPEC):
- All stat card icons: `aria-hidden="true"`
- Motivational message: use `<p>` tag (or `<h1>` if this is the page's primary heading -- since the page no longer has a separate h1, make this `<h1>` for semantic correctness)
- All foreground text must meet WCAG AA contrast against the gradient background

**2. Update `apps/web/app/page.tsx`**:

Replace the entire placeholder content with:

```typescript
import { HeroImpactSection } from "@/components/dashboard/hero-impact-section";

export default function DashboardPage() {
  return (
    <div className="space-y-6">
      <HeroImpactSection />
    </div>
  );
}
```

The page becomes a clean container that renders the HeroImpactSection as its first (and currently only) child. The `space-y-6` class is preserved for future sections (live feed, etc.) that will be added in Phase 3 and 4. Remove the old placeholder Card, h1, and subtitle.
  </action>
  <verify>
    <automated>cd /Users/bzcruz/Projects/trash-detection-dashboard/apps/web && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/dashboard/hero-impact-section.tsx contains `export function HeroImpactSection`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `import { heroMockData, formatNumber } from "@/lib/mock-data"`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `import { HeroIllustration } from "@/components/dashboard/hero-illustration"`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `import { Recycle, Leaf, Package } from "lucide-react"`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `Great job`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `of waste today`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `eco-green`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `grid-cols-3`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `grid-cols-1`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `Recycled`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `CO2 Saved`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `Total Items Sorted`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `aria-hidden="true"`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `min-h-[280px]`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `rounded-xl`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `shadow-sm`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `z-10`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `text-muted-foreground`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `max-w-lg`
    - apps/web/app/page.tsx contains `import { HeroImpactSection } from "@/components/dashboard/hero-impact-section"`
    - apps/web/app/page.tsx contains `<HeroImpactSection`
    - apps/web/app/page.tsx does NOT contain `Welcome to Eco Dashboard` (old placeholder removed)
    - apps/web/app/globals.css contains `hero-gradient` (dark mode gradient class)
    - TypeScript compilation (`npx tsc --noEmit`) exits with 0
    - Production build (`npm run build`) exits with 0
  </acceptance_criteria>
  <done>
    - HeroImpactSection renders motivational message "Great job! You've diverted 12.4 kg of waste today." with 12.4 kg in eco-green
    - 3 stat cards display: Recycled 68%, CO2 Saved 21 kg, Total Items Sorted 241
    - Each stat card has a colored Lucide icon (Recycle, Leaf, Package) in a rounded-lg container
    - Hero card has gradient background (sky-blue to teal to green) with dark mode variant
    - Dashboard page renders HeroImpactSection as its first and only child (old placeholder removed)
    - Responsive: 3-col grid on desktop, 1-col on mobile, text sizes adjust
    - Full production build passes with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="soft">
  <name>Task 3: Verify hero section visual appearance</name>
  <files>apps/web/app/page.tsx</files>
  <action>
    Start the dev server and visually verify the hero impact section matches the design contract. The executor should run `npm run dev` and open the browser to http://localhost:3000.
  </action>
  <what-built>
    Hero Impact Section with motivational message, 3 stat cards, illustrated SVG background with floating leaf animations, responsive layout, and eco-gradient. Built per the dashboard-reference.png mockup and UI-SPEC.
  </what-built>
  <how-to-verify>
    1. Run `cd /Users/bzcruz/Projects/trash-detection-dashboard/apps/web && npm run dev`
    2. Open http://localhost:3000 in browser
    3. Verify: Hero section is the first visible element on the dashboard
    4. Verify: Motivational message reads "Great job! You've diverted 12.4 kg of waste today." with "12.4 kg" in green
    5. Verify: 3 stat cards below the message show "Recycled 68%", "CO2 Saved 21 kg", "Total Items Sorted 241"
    6. Verify: Each stat card has a colored icon (green recycle, teal leaf, blue package)
    7. Verify: Background has a subtle sky-to-green gradient with cloud and leaf SVG illustrations
    8. Verify: Leaves gently float/bob up and down
    9. Verify: Resize browser to mobile width -- stat cards stack vertically, text shrinks
    10. Verify: Overall feel is inspiring, premium, and eco-themed -- not cartoonish or sterile
  </how-to-verify>
  <verify>
    <automated>cd /Users/bzcruz/Projects/trash-detection-dashboard/apps/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms the hero section looks correct and matches the design intent</done>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` -- zero TypeScript errors
2. `cd apps/web && npm run build` -- production build succeeds
3. All 4 new files exist: lib/mock-data.ts, globals.css (modified), components/dashboard/hero-illustration.tsx, components/dashboard/hero-impact-section.tsx
4. Dashboard page (app/page.tsx) renders HeroImpactSection as first child
5. No old placeholder content remains on the dashboard page
</verification>

<success_criteria>
- Dashboard homepage displays the hero impact section as the first visible element above the fold
- Hero shows motivational message "Great job! You've diverted 12.4 kg of waste today." with dynamic value in eco-green
- 3 stat cards show: Recycled 68%, CO2 Saved 21 kg, Total Items Sorted 241 -- each with a colored Lucide icon
- Illustrated SVG background (sky, clouds, floating leaves -- no globe) with CSS animations
- Eco-gradient card background (light blue -> teal -> green) with dark mode support
- Responsive layout: 3-column grid on desktop, vertical stack on mobile
- Clean typography hierarchy: Display (28px) for headline, Heading (20px) for values, Body (14px) for labels
- Animations respect prefers-reduced-motion
- Production build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-impact-section/02-01-SUMMARY.md`
</output>
