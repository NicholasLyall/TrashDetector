---
phase: 10-visual-polish-and-demo-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/demo.ts
  - apps/web/components/dashboard/demo-button.tsx
  - apps/web/components/dashboard/live-feed-section.tsx
  - apps/web/components/dashboard/hero-impact-section.tsx
  - apps/web/components/layout/top-bar.tsx
  - apps/web/hooks/use-metrics.ts
  - apps/web/hooks/use-events.ts
  - apps/web/app/globals.css
autonomous: true
requirements:
  - VISL-05
must_haves:
  truths:
    - "Presenter can trigger a demo sort event from the dashboard UI without switching to terminal"
    - "Hero metrics and KPI chips animate smoothly when values change (count up/down)"
    - "Latest sorted item card shows a subtle green pulse when a new event arrives"
    - "SWR caches revalidate immediately after demo event so dashboard updates within 2 seconds"
  artifacts:
    - path: "apps/web/lib/demo.ts"
      provides: "triggerDemoEvent function that POSTs multipart form data to /events"
      exports: ["triggerDemoEvent", "DEMO_ITEMS"]
    - path: "apps/web/components/dashboard/demo-button.tsx"
      provides: "Visible demo button component with loading state"
      exports: ["DemoButton"]
    - path: "apps/web/app/globals.css"
      provides: "pulse-highlight keyframe animation for new event indication"
      contains: "@keyframes pulse-highlight"
  key_links:
    - from: "apps/web/components/dashboard/demo-button.tsx"
      to: "apps/web/lib/demo.ts"
      via: "import triggerDemoEvent"
      pattern: "triggerDemoEvent"
    - from: "apps/web/components/dashboard/demo-button.tsx"
      to: "SWR cache"
      via: "useSWRConfig().mutate after POST"
      pattern: "mutate"
    - from: "apps/web/components/dashboard/hero-impact-section.tsx"
      to: "AnimatedNumber component"
      via: "useEffect + requestAnimationFrame counter"
      pattern: "AnimatedNumber"
---

<objective>
Add interactive demo capabilities and animated polish to the dashboard: a demo button that triggers sort events via the backend, animated number counters on hero metrics and KPI chips, and a pulse highlight on newly arrived feed items.

Purpose: Makes the dashboard demo-ready for judges -- the presenter can trigger events live from the UI and see smooth, polished visual feedback as metrics update.
Output: Demo button component, animated counter utility, pulse highlight CSS, updated hero/feed/top-bar components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-polish-and-demo-readiness/10-CONTEXT.md

@apps/web/lib/api.ts
@apps/web/lib/types.ts
@apps/web/lib/mock-data.ts
@apps/web/hooks/use-metrics.ts
@apps/web/hooks/use-events.ts
@apps/web/components/dashboard/hero-impact-section.tsx
@apps/web/components/dashboard/live-feed-section.tsx
@apps/web/components/dashboard/latest-item-card.tsx
@apps/web/components/layout/top-bar.tsx
@apps/web/app/globals.css
@apps/web/components/providers/swr-provider.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/web/lib/api.ts:
```typescript
export const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000";
export async function fetcher<T>(path: string): Promise<T>;
```

From apps/web/lib/types.ts:
```typescript
export type WasteCategory = "paper_cardboard" | "metal_glass" | "plastic" | "trash";
export interface MetricsData {
  readonly total_items: number;
  readonly recycling_rate: number;
  readonly co2_saved_kg: number;
  readonly waste_diverted_kg: number;
  readonly avg_confidence: number;
  readonly uncertain_rate: number;
  readonly fallback_rate: number;
}
```

From apps/web/hooks/use-metrics.ts:
```typescript
export function useMetrics(): { metrics: MetricsData | undefined; error: any; isLoading: boolean };
```

From apps/web/hooks/use-events.ts:
```typescript
export function useEvents(limit?: number): { events: readonly SortEvent[] | undefined; error: any; isLoading: boolean };
```

Backend POST /events expects multipart form data:
- label: str (Form) -- one of "paper_cardboard", "metal_glass", "plastic", "trash"
- confidence: float (Form) -- 0.0-1.0
- source_device_id: str (Form) -- e.g., "pi-001"
- image: UploadFile (File) -- required, JPEG or PNG

Backend VALID_CATEGORIES: ("paper_cardboard", "metal_glass", "plastic", "trash")
Backend CONFIDENCE_THRESHOLD: 0.7 (below this, routes to trash bin)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demo event trigger, animated counter, and demo button</name>
  <files>
    apps/web/lib/demo.ts,
    apps/web/components/dashboard/demo-button.tsx,
    apps/web/components/dashboard/animated-number.tsx,
    apps/web/hooks/use-metrics.ts,
    apps/web/hooks/use-events.ts
  </files>
  <read_first>
    apps/web/lib/api.ts,
    apps/web/lib/types.ts,
    apps/web/lib/mock-data.ts,
    apps/web/hooks/use-metrics.ts,
    apps/web/hooks/use-events.ts,
    apps/web/components/providers/swr-provider.tsx,
    apps/api/routers/events.py,
    apps/api/constants.py
  </read_first>
  <action>
    **1. Create `apps/web/lib/demo.ts`** -- Demo event trigger utility (per D-01: demo events from dashboard UI).

    Export a `DEMO_ITEMS` array of 8 objects, each with:
    - `label`: one of the 4 WasteCategory values (2 of each)
    - `name`: human-readable name (e.g., "Plastic Bottle", "Cardboard Box")
    - `confidence`: float between 0.72 and 0.97 (all above 0.7 threshold so they route correctly)

    Export `async function triggerDemoEvent(): Promise<{ id: string; message: string }>` that:
    1. Picks a random item from `DEMO_ITEMS`
    2. Creates a 1x1 pixel transparent PNG as a Blob (since backend requires an image file). Use `new Blob([new Uint8Array([...])], { type: "image/png" })` with the minimal 67-byte valid PNG bytes: `[137,80,78,71,13,10,26,10,0,0,0,13,73,72,68,82,0,0,0,1,0,0,0,1,8,6,0,0,0,31,21,196,137,0,0,0,10,73,68,65,84,120,156,98,0,0,0,6,0,5,0,1,13,10,45,180,0,0,0,0,73,69,78,68,174,66,96,130]`
    3. Builds a `FormData` with fields: `label`, `confidence` (as string), `source_device_id` = `"demo-dashboard"`, and `image` (as File with name `"demo.png"`)
    4. POSTs to `${API_BASE_URL}/events` with the FormData (no Content-Type header -- browser sets multipart boundary)
    5. Checks `res.ok`, throws Error on failure
    6. Returns parsed JSON `{ id, message }`

    Import `API_BASE_URL` from `@/lib/api`.

    **2. Create `apps/web/components/dashboard/demo-button.tsx`** -- "use client" component (per D-01).

    Import `triggerDemoEvent` from `@/lib/demo`, `useSWRConfig` from `swr`, `Zap` icon from `lucide-react`, `Button` from `@/components/ui/button`.

    Component `DemoButton` renders a shadcn Button (variant="default", size="sm") with:
    - Emerald-600 background (`className="bg-emerald-600 hover:bg-emerald-700 text-white gap-1.5"`)
    - `<Zap className="h-4 w-4" />` icon + text "Sort Item"
    - `disabled` when `isLoading` state is true
    - Shows "Sorting..." text when loading
    - `onClick` handler that:
      1. Sets `isLoading = true`
      2. Calls `await triggerDemoEvent()`
      3. Calls `mutate(() => true)` from `useSWRConfig()` to revalidate all SWR keys (forces immediate refresh of events, metrics, breakdown)
      4. Sets `isLoading = false` in finally block
      5. Catches errors and logs to console.error (no toast per D-04)

    **3. Create `apps/web/components/dashboard/animated-number.tsx`** -- "use client" component (per D-06).

    Export `AnimatedNumber` component with props:
    - `value: number` -- target value
    - `duration?: number` -- animation ms, default 600
    - `formatter?: (n: number) => string` -- display formatter, default `Math.round(n).toLocaleString()`

    Implementation:
    - Use `useRef` to track previous value, `useState` for displayed value
    - On `value` change, animate from previous to new using `requestAnimationFrame` loop
    - Use ease-out timing: `t = 1 - Math.pow(1 - progress, 3)` where `progress = elapsed / duration`
    - Apply `formatter` to the interpolated value at each frame
    - Respect `prefers-reduced-motion`: check `window.matchMedia("(prefers-reduced-motion: reduce)").matches` and skip animation if true (jump to final value)
    - Render as `<span>` with `tabIndex={-1}` and `aria-live="polite"` for accessibility
    - Clean up animation frame on unmount

    **4. Update `apps/web/hooks/use-metrics.ts`** -- Expose `mutate` function.

    Change from:
    ```typescript
    const { data, error, isLoading } = useSWR<MetricsData>("/metrics");
    return { metrics: data, error, isLoading } as const;
    ```
    To:
    ```typescript
    const { data, error, isLoading, mutate } = useSWR<MetricsData>("/metrics");
    return { metrics: data, error, isLoading, mutate } as const;
    ```

    **5. Update `apps/web/hooks/use-events.ts`** -- Expose `mutate` function.

    Change from:
    ```typescript
    const { data, error, isLoading } = useSWR<readonly SortEvent[]>(`/events?limit=${limit}`);
    return { events: data, error, isLoading } as const;
    ```
    To:
    ```typescript
    const { data, error, isLoading, mutate } = useSWR<readonly SortEvent[]>(`/events?limit=${limit}`);
    return { events: data, error, isLoading, mutate } as const;
    ```
  </action>
  <verify>
    <automated>cd /Users/bzcruz/Projects/trash-detection-dashboard && cat apps/web/lib/demo.ts | head -5 && cat apps/web/components/dashboard/demo-button.tsx | head -5 && cat apps/web/components/dashboard/animated-number.tsx | head -5 && grep -c "mutate" apps/web/hooks/use-metrics.ts && grep -c "mutate" apps/web/hooks/use-events.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/demo.ts contains `export const DEMO_ITEMS` and `export async function triggerDemoEvent`
    - apps/web/lib/demo.ts contains `API_BASE_URL` import from `@/lib/api`
    - apps/web/lib/demo.ts contains `new FormData()` and `source_device_id`
    - apps/web/components/dashboard/demo-button.tsx contains `export function DemoButton`
    - apps/web/components/dashboard/demo-button.tsx contains `useSWRConfig` and `mutate`
    - apps/web/components/dashboard/demo-button.tsx contains `triggerDemoEvent`
    - apps/web/components/dashboard/animated-number.tsx contains `export function AnimatedNumber`
    - apps/web/components/dashboard/animated-number.tsx contains `requestAnimationFrame`
    - apps/web/components/dashboard/animated-number.tsx contains `prefers-reduced-motion`
    - apps/web/hooks/use-metrics.ts contains `mutate` in the return object
    - apps/web/hooks/use-events.ts contains `mutate` in the return object
  </acceptance_criteria>
  <done>
    Demo trigger utility creates valid multipart POST to /events. DemoButton component triggers demo event and revalidates all SWR caches. AnimatedNumber component smoothly animates between values with reduced-motion support. Both hooks expose mutate for manual revalidation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire demo button and animated counters into dashboard, add pulse highlight</name>
  <files>
    apps/web/components/dashboard/hero-impact-section.tsx,
    apps/web/components/dashboard/live-feed-section.tsx,
    apps/web/components/dashboard/latest-item-card.tsx,
    apps/web/components/layout/top-bar.tsx,
    apps/web/app/globals.css
  </files>
  <read_first>
    apps/web/components/dashboard/hero-impact-section.tsx,
    apps/web/components/dashboard/live-feed-section.tsx,
    apps/web/components/dashboard/latest-item-card.tsx,
    apps/web/components/layout/top-bar.tsx,
    apps/web/app/globals.css,
    apps/web/components/dashboard/animated-number.tsx,
    apps/web/components/dashboard/demo-button.tsx,
    apps/web/lib/compute-metrics.ts,
    apps/web/hooks/use-metrics.ts
  </read_first>
  <action>
    **1. Add pulse-highlight animation to `apps/web/app/globals.css`** (per D-04: subtle green pulse on new items).

    Add after the existing `@layer utilities` block:

    ```css
    @keyframes pulse-highlight {
      0% {
        box-shadow: 0 0 0 0 hsl(142 71% 45% / 0.4);
      }
      70% {
        box-shadow: 0 0 0 8px hsl(142 71% 45% / 0);
      }
      100% {
        box-shadow: 0 0 0 0 hsl(142 71% 45% / 0);
      }
    }

    @layer utilities {
      .animate-pulse-highlight {
        animation: pulse-highlight 1s ease-out;
      }
    }
    ```

    Also add `prefers-reduced-motion` for the new animation:
    Update the existing `@media (prefers-reduced-motion: reduce)` block to include `.animate-pulse-highlight`.

    **2. Update `apps/web/components/dashboard/latest-item-card.tsx`** (per D-04: pulse on new event).

    Add a `readonly isNew?: boolean` prop to `LatestItemCardProps`.
    When `isNew` is true, add `animate-pulse-highlight` class to the outer `<Card>` element.
    Use a `useEffect` + `useState` pattern to auto-remove the animation after 1s:
    - `const [showPulse, setShowPulse] = useState(false)`
    - When `event.id` changes (use `useEffect` with `[event.id]` dependency), set `showPulse = true`, then `setTimeout(() => setShowPulse(false), 1000)`
    - Apply `animate-pulse-highlight` class only when `showPulse && isNew` is true
    - Add `"use client"` directive at top of file (needed for hooks)

    **3. Update `apps/web/components/dashboard/live-feed-section.tsx`** (per D-01: demo button placement, D-04: pulse indication).

    Add the `DemoButton` component next to the "Live Feed" title. Import `DemoButton` from `@/components/dashboard/demo-button`.

    Place it in the CardHeader, to the right of the tree equivalence badge, using `ml-2`:
    ```tsx
    <DemoButton />
    ```

    Track the previous latest event ID to detect new arrivals:
    - Add `const prevLatestIdRef = useRef<string | null>(null)`
    - Compute `const isNewEvent = latestEvent != null && latestEvent.id !== prevLatestIdRef.current`
    - Update `prevLatestIdRef.current = latestEvent?.id ?? null` in a `useEffect` with `[latestEvent?.id]` dependency
    - Pass `isNew={isNewEvent}` to `<LatestItemCard>`

    **4. Update `apps/web/components/dashboard/hero-impact-section.tsx`** (per D-06: animated counters).

    Import `AnimatedNumber` from `@/components/dashboard/animated-number`.

    Replace the static value strings in the `statCards` array with `AnimatedNumber` components:
    - Recycled: `<AnimatedNumber value={Math.round((metrics?.recycling_rate ?? 0) * 100)} formatter={(n) => \`${Math.round(n)}%\`} />`
    - CO2 Saved: `<AnimatedNumber value={metrics?.co2_saved_kg ?? 0} formatter={(n) => \`${n.toFixed(1)} kg\`} />`
    - Total Items: `<AnimatedNumber value={metrics?.total_items ?? 0} />`

    Update the `StatCard` component to accept `value: React.ReactNode` instead of `value: string` so it can render the AnimatedNumber components.

    Also animate the hero headline waste diverted number:
    Replace `{metrics?.waste_diverted_kg ?? 0} kg` in the h1 with:
    ```tsx
    <AnimatedNumber value={metrics?.waste_diverted_kg ?? 0} formatter={(n) => `${n.toFixed(1)} kg`} />
    ```

    **5. Update `apps/web/components/layout/top-bar.tsx`** (per D-06: animated KPI counters).

    Import `useMetrics` from `@/hooks/use-metrics` and `AnimatedNumber` from `@/components/dashboard/animated-number`.

    Replace `computeKpiMetrics(sortEventsMockData)` with live data from `useMetrics()`:
    - `const { metrics } = useMetrics();`
    - Remove import of `computeKpiMetrics` and `sortEventsMockData`

    Update KpiChip component to accept `value: React.ReactNode` instead of `value: string`.

    Wire the 4 KPI chips to live metrics with AnimatedNumber:
    - Total Items: `<AnimatedNumber value={metrics?.total_items ?? 0} />`
    - Recycling Rate: `<AnimatedNumber value={Math.round((metrics?.recycling_rate ?? 0) * 100)} formatter={(n) => \`${Math.round(n)}%\`} />`
    - Avg Confidence: `<AnimatedNumber value={Math.round((metrics?.avg_confidence ?? 0) * 100)} formatter={(n) => \`${Math.round(n)}%\`} />`
    - Fallback Rate: `<AnimatedNumber value={Math.round((metrics?.fallback_rate ?? 0) * 100)} formatter={(n) => \`${Math.round(n)}%\`} />`
  </action>
  <verify>
    <automated>cd /Users/bzcruz/Projects/trash-detection-dashboard && grep "pulse-highlight" apps/web/app/globals.css && grep "AnimatedNumber" apps/web/components/dashboard/hero-impact-section.tsx && grep "DemoButton" apps/web/components/dashboard/live-feed-section.tsx && grep "AnimatedNumber" apps/web/components/layout/top-bar.tsx && grep "isNew" apps/web/components/dashboard/latest-item-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/globals.css contains `@keyframes pulse-highlight` with `box-shadow: 0 0 0 0 hsl(142 71% 45%`
    - apps/web/app/globals.css contains `.animate-pulse-highlight` in a `@layer utilities` block
    - apps/web/app/globals.css prefers-reduced-motion block includes `animate-pulse-highlight`
    - apps/web/components/dashboard/latest-item-card.tsx contains `isNew` prop and `animate-pulse-highlight`
    - apps/web/components/dashboard/latest-item-card.tsx contains `"use client"` directive
    - apps/web/components/dashboard/live-feed-section.tsx contains `import { DemoButton }` and `<DemoButton`
    - apps/web/components/dashboard/live-feed-section.tsx contains `prevLatestIdRef`
    - apps/web/components/dashboard/hero-impact-section.tsx contains `import { AnimatedNumber }` and at least 3 `<AnimatedNumber` instances
    - apps/web/components/dashboard/hero-impact-section.tsx StatCard value prop type is `React.ReactNode` (not `string`)
    - apps/web/components/layout/top-bar.tsx contains `import { useMetrics }` and `import { AnimatedNumber }`
    - apps/web/components/layout/top-bar.tsx does NOT contain `computeKpiMetrics` or `sortEventsMockData`
    - apps/web/components/layout/top-bar.tsx KpiChip value prop type is `React.ReactNode` (not `string`)
  </acceptance_criteria>
  <done>
    Demo button visible in live feed header. Clicking it triggers a backend event and dashboard updates within 2 seconds. Hero metrics and KPI chips animate smoothly when values change. Latest item card shows green pulse on new event arrival. All animations respect prefers-reduced-motion.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "AnimatedNumber" apps/web/components/` returns matches in hero-impact-section.tsx, top-bar.tsx, and animated-number.tsx
2. `grep -r "DemoButton" apps/web/components/` returns matches in demo-button.tsx and live-feed-section.tsx
3. `grep "pulse-highlight" apps/web/app/globals.css` returns keyframe and utility class
4. `grep "mutate" apps/web/hooks/use-metrics.ts apps/web/hooks/use-events.ts` returns matches in both hooks
5. `npx next build` (or `npx tsc --noEmit`) compiles without type errors
</verification>

<success_criteria>
- DemoButton is visible on dashboard and triggers POST /events with valid multipart form data
- Hero stat values animate smoothly between old and new values when metrics change
- KPI chips in top bar show live data (not mock) with animated transitions
- Latest item card pulses green briefly when a new event is detected
- All animations are suppressed when prefers-reduced-motion is active
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-polish-and-demo-readiness/10-01-SUMMARY.md`
</output>
