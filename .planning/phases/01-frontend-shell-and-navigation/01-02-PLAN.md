---
phase: 01-frontend-shell-and-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/web/components/layout/sidebar.tsx
  - apps/web/components/layout/top-bar.tsx
  - apps/web/components/layout/app-shell.tsx
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/analytics/page.tsx
  - apps/web/app/impact/page.tsx
  - apps/web/app/model/page.tsx
  - apps/web/app/settings/page.tsx
  - apps/web/lib/navigation.ts
autonomous: true
requirements:
  - DASH-01
  - VISL-02

must_haves:
  truths:
    - "App loads with a left sidebar showing ECO DASHBOARD branding and recycling leaf icon"
    - "Sidebar shows navigation links for Dashboard, Analytics, Impact, Model, and Settings with icons"
    - "Sidebar shows Admin user with avatar at the bottom"
    - "Clicking each nav item routes to the correct page"
    - "Active nav item is visually highlighted in the sidebar"
    - "Top bar shows hamburger menu, Smart Dashboard title, and user avatar"
    - "All pages render placeholder content with page title"
    - "UI uses shadcn/ui components with consistent eco-tech styling"
  artifacts:
    - path: "apps/web/components/layout/sidebar.tsx"
      provides: "Left sidebar with ECO DASHBOARD branding, nav links, admin user"
      contains: "ECO DASHBOARD"
      min_lines: 50
    - path: "apps/web/components/layout/top-bar.tsx"
      provides: "Top bar with hamburger menu, title, user avatar"
      contains: "Smart Dashboard"
      min_lines: 20
    - path: "apps/web/components/layout/app-shell.tsx"
      provides: "Layout wrapper combining sidebar + top bar + main content"
      contains: "AppShell"
      min_lines: 15
    - path: "apps/web/lib/navigation.ts"
      provides: "Navigation configuration with route paths, labels, and icons"
      exports: ["NAV_ITEMS"]
      min_lines: 15
    - path: "apps/web/app/analytics/page.tsx"
      provides: "Analytics page route"
      contains: "Analytics"
    - path: "apps/web/app/impact/page.tsx"
      provides: "Impact page route"
      contains: "Impact"
    - path: "apps/web/app/model/page.tsx"
      provides: "Model page route"
      contains: "Model"
    - path: "apps/web/app/settings/page.tsx"
      provides: "Settings page route"
      contains: "Settings"
  key_links:
    - from: "apps/web/components/layout/sidebar.tsx"
      to: "apps/web/lib/navigation.ts"
      via: "imports NAV_ITEMS for rendering nav links"
      pattern: "import.*NAV_ITEMS.*from.*navigation"
    - from: "apps/web/components/layout/sidebar.tsx"
      to: "apps/web/app/*/page.tsx"
      via: "Next.js Link component with href from NAV_ITEMS"
      pattern: "Link.*href"
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/components/layout/app-shell.tsx"
      via: "Root layout wraps children in AppShell"
      pattern: "AppShell"
    - from: "apps/web/components/layout/app-shell.tsx"
      to: "apps/web/components/layout/sidebar.tsx"
      via: "AppShell renders Sidebar component"
      pattern: "Sidebar"
    - from: "apps/web/components/layout/app-shell.tsx"
      to: "apps/web/components/layout/top-bar.tsx"
      via: "AppShell renders TopBar component"
      pattern: "TopBar"
---

<objective>
Build the SaaS application shell with left sidebar navigation, top bar, routing for all 5 pages, and eco-tech styling.

Purpose: Deliver the core DASH-01 requirement -- a professional SaaS layout that judges immediately recognize as a real product. This shell is the visual frame for all subsequent dashboard content.
Output: A navigable multi-page app with sidebar, top bar, and placeholder pages for Dashboard, Analytics, Impact, Model, and Settings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/UI_UX_SPEC.md
@docs/dashboard-reference.png

@.planning/phases/01-frontend-shell-and-navigation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 -- shadcn/ui components available to this plan -->

From apps/web/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From apps/web/components/ui/button.tsx:
```typescript
export { Button, buttonVariants }
// variants: "default" | "destructive" | "outline" | "secondary" | "ghost" | "link"
// sizes: "default" | "sm" | "lg" | "icon"
```

From apps/web/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent }
```

From apps/web/components/ui/badge.tsx:
```typescript
export { Badge, badgeVariants }
```

From apps/web/components/ui/tooltip.tsx:
```typescript
export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider }
```

From apps/web/components/ui/separator.tsx:
```typescript
export { Separator }
```

From apps/web/components/ui/avatar.tsx:
```typescript
export { Avatar, AvatarImage, AvatarFallback }
```

From apps/web/components/ui/sheet.tsx:
```typescript
export { Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle, SheetDescription }
```

CSS variables available (from globals.css):
```css
--eco-green: 142 71% 45%;
--eco-teal: 174 62% 47%;
--eco-blue: 199 89% 48%;
--eco-bg: 200 40% 94%;
--eco-card: 0 0% 100%;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create navigation config and layout components (Sidebar, TopBar, AppShell)</name>
  <files>
    apps/web/lib/navigation.ts,
    apps/web/components/layout/sidebar.tsx,
    apps/web/components/layout/top-bar.tsx,
    apps/web/components/layout/app-shell.tsx,
    apps/web/app/layout.tsx
  </files>
  <read_first>
    apps/web/app/layout.tsx,
    apps/web/app/globals.css,
    apps/web/lib/utils.ts,
    apps/web/components/ui/button.tsx,
    apps/web/components/ui/avatar.tsx,
    apps/web/components/ui/separator.tsx,
    apps/web/components/ui/tooltip.tsx,
    apps/web/components/ui/sheet.tsx,
    docs/UI_UX_SPEC.md,
    docs/dashboard-reference.png
  </read_first>
  <action>
    **Step 1: Create apps/web/lib/navigation.ts**

    Define the navigation configuration as a readonly array of objects:

    ```typescript
    import {
      LayoutDashboard,
      BarChart3,
      Leaf,
      Brain,
      Settings,
      type LucideIcon,
    } from "lucide-react";

    export type NavItem = {
      readonly label: string;
      readonly href: string;
      readonly icon: LucideIcon;
    };

    export const NAV_ITEMS: readonly NavItem[] = [
      { label: "Dashboard", href: "/", icon: LayoutDashboard },
      { label: "Analytics", href: "/analytics", icon: BarChart3 },
      { label: "Impact", href: "/impact", icon: Leaf },
      { label: "Model", href: "/model", icon: Brain },
      { label: "Settings", href: "/settings", icon: Settings },
    ] as const;
    ```

    Install lucide-react if not already installed: `cd apps/web && npm install lucide-react`

    **Step 2: Create apps/web/components/layout/sidebar.tsx**

    Build a client component ("use client" at top) for the left sidebar. Reference the mockup image for visual design.

    Structure:
    - Fixed width: w-64 (256px) on desktop, hidden on mobile (controlled via prop or responsive)
    - Full height: h-screen, fixed position
    - Background: white (bg-white)
    - Left border: subtle border-r border-gray-200

    Content from top to bottom:
    1. **Branding block** (top):
       - Green recycling leaf icon (use the Recycle icon from lucide-react)
       - "ECO" in bold green text + "DASHBOARD" in normal text below or beside it
       - Padding: p-6 at top
       - Separated from nav by a Separator component

    2. **Navigation links** (middle, flex-1):
       - Map over NAV_ITEMS from lib/navigation.ts
       - Each item is a Next.js Link wrapped in a button-like element
       - Show icon (20x20) + label text
       - Use `usePathname()` from `next/navigation` to determine active state
       - Active state: bg-emerald-50 text-emerald-700 font-medium with rounded-lg
       - Inactive state: text-gray-600 hover:bg-gray-50 hover:text-gray-900 with rounded-lg
       - Each nav item: py-2.5 px-3 with gap-3 between icon and text
       - Use Tooltip wrapping each item for accessibility

    3. **Admin user block** (bottom):
       - Separated from nav by a Separator component
       - Show Avatar with AvatarFallback "AD" (for Admin) with green background
       - "Admin" text beside the avatar
       - Padding: p-4 at bottom

    Export as named export: `export function Sidebar()`

    **Step 3: Create apps/web/components/layout/top-bar.tsx**

    Build a client component for the top bar. Reference the mockup: it shows a hamburger menu icon, "Smart Dashboard" title with dropdown chevron, KPI chip strip area (empty for now -- Phase 4 fills this), and user avatar.

    Structure:
    - Height: h-16
    - Background: white bg-white
    - Bottom border: border-b border-gray-200
    - Flex row with items centered, justify-between
    - Padding: px-4 md:px-6

    Content left to right:
    1. **Mobile menu button** (visible only on md:hidden):
       - Use the Menu icon from lucide-react
       - Button variant="ghost" size="icon"
       - onClick triggers a Sheet overlay containing the Sidebar content for mobile
       - Use the Sheet component from shadcn/ui for mobile sidebar drawer

    2. **Title section**:
       - "Smart Dashboard" text in font-semibold text-gray-800
       - Small ChevronDown icon from lucide-react beside it (decorative, no dropdown action needed yet)

    3. **KPI chip strip placeholder**:
       - An empty div with id="kpi-strip" and className="hidden md:flex items-center gap-2 flex-1 justify-center"
       - This will be populated in Phase 4 (DASH-04)

    4. **User avatar** (right side):
       - Avatar component with AvatarFallback "AD"
       - Small size: h-8 w-8

    Accept an `onMobileMenuOpen` prop of type `() => void` for toggling mobile sidebar.

    Export as named export: `export function TopBar({ onMobileMenuOpen }: { onMobileMenuOpen: () => void })`

    **Step 4: Create apps/web/components/layout/app-shell.tsx**

    Build a client component that composes Sidebar + TopBar + main content area.

    Structure:
    ```
    <div className="flex h-screen">
      {/* Desktop sidebar - hidden on mobile */}
      <div className="hidden md:block">
        <Sidebar />
      </div>

      {/* Mobile sidebar via Sheet */}
      <Sheet open={sidebarOpen} onOpenChange={setSidebarOpen}>
        <SheetContent side="left" className="p-0 w-64">
          <SheetHeader className="sr-only">
            <SheetTitle>Navigation</SheetTitle>
          </SheetHeader>
          <Sidebar />
        </SheetContent>
      </Sheet>

      {/* Main area */}
      <div className="flex-1 flex flex-col min-h-screen overflow-hidden">
        <TopBar onMobileMenuOpen={() => setSidebarOpen(true)} />
        <main className="flex-1 overflow-y-auto p-6" style={{ backgroundColor: "hsl(var(--eco-bg))" }}>
          {children}
        </main>
      </div>
    </div>
    ```

    Use React.useState for `sidebarOpen` boolean state.
    Accept `children: React.ReactNode` prop.

    Export as named export: `export function AppShell({ children }: { children: React.ReactNode })`

    **Step 5: Update apps/web/app/layout.tsx**

    Wrap children in `<AppShell>` and add the `<TooltipProvider>` wrapper:

    ```tsx
    import { AppShell } from "@/components/layout/app-shell";
    import { TooltipProvider } from "@/components/ui/tooltip";

    // ... existing metadata ...

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="en">
          <body className={/* existing font classes */}>
            <TooltipProvider>
              <AppShell>{children}</AppShell>
            </TooltipProvider>
          </body>
        </html>
      );
    }
    ```

    Keep the existing metadata (title, description) from Plan 01.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/navigation.ts contains "export const NAV_ITEMS"
    - apps/web/lib/navigation.ts contains "Dashboard" and "Analytics" and "Impact" and "Model" and "Settings"
    - apps/web/lib/navigation.ts contains href values "/" and "/analytics" and "/impact" and "/model" and "/settings"
    - apps/web/components/layout/sidebar.tsx contains "ECO" (branding text)
    - apps/web/components/layout/sidebar.tsx contains "DASHBOARD" (branding text)
    - apps/web/components/layout/sidebar.tsx contains "NAV_ITEMS" (imports from navigation)
    - apps/web/components/layout/sidebar.tsx contains "usePathname" (active state detection)
    - apps/web/components/layout/sidebar.tsx contains "Admin" (user block)
    - apps/web/components/layout/sidebar.tsx contains "Avatar" (uses shadcn/ui Avatar)
    - apps/web/components/layout/top-bar.tsx contains "Smart Dashboard"
    - apps/web/components/layout/top-bar.tsx contains "onMobileMenuOpen"
    - apps/web/components/layout/app-shell.tsx contains "Sidebar"
    - apps/web/components/layout/app-shell.tsx contains "TopBar"
    - apps/web/components/layout/app-shell.tsx contains "Sheet" (mobile sidebar)
    - apps/web/components/layout/app-shell.tsx contains "eco-bg" (eco background color)
    - apps/web/app/layout.tsx contains "AppShell"
    - apps/web/app/layout.tsx contains "TooltipProvider"
    - `cd apps/web && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>
    Sidebar renders with ECO DASHBOARD branding, recycling icon, 5 nav links with icons, active state highlighting via usePathname, and Admin user at bottom. TopBar renders with hamburger menu (mobile), "Smart Dashboard" title, KPI placeholder area, and user avatar. AppShell composes them with mobile Sheet drawer. Root layout wraps all pages in AppShell.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder pages for all 5 routes</name>
  <files>
    apps/web/app/page.tsx,
    apps/web/app/analytics/page.tsx,
    apps/web/app/impact/page.tsx,
    apps/web/app/model/page.tsx,
    apps/web/app/settings/page.tsx
  </files>
  <read_first>
    apps/web/app/page.tsx,
    apps/web/components/ui/card.tsx,
    apps/web/lib/navigation.ts,
    apps/web/lib/utils.ts
  </read_first>
  <action>
    Create or update 5 page files. Each page is a server component (no "use client" needed).

    **apps/web/app/page.tsx** (Dashboard -- update existing):
    ```tsx
    import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

    export default function DashboardPage() {
      return (
        <div className="space-y-6">
          <div>
            <h1 className="text-2xl font-bold text-gray-900">Dashboard</h1>
            <p className="text-gray-500 mt-1">Real-time waste sorting intelligence</p>
          </div>
          <Card>
            <CardHeader>
              <CardTitle>Welcome to Eco Dashboard</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-gray-600">
                Hero impact section, live feed, waste composition, and model performance will appear here.
              </p>
            </CardContent>
          </Card>
        </div>
      );
    }
    ```

    **apps/web/app/analytics/page.tsx**:
    ```tsx
    import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

    export default function AnalyticsPage() {
      return (
        <div className="space-y-6">
          <div>
            <h1 className="text-2xl font-bold text-gray-900">Analytics</h1>
            <p className="text-gray-500 mt-1">Detailed sorting trends and category breakdowns</p>
          </div>
          <Card>
            <CardHeader>
              <CardTitle>Analytics Overview</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-gray-600">
                Category breakdown charts, time-based trends, and filterable event lists will appear here.
              </p>
            </CardContent>
          </Card>
        </div>
      );
    }
    ```

    **apps/web/app/impact/page.tsx**:
    ```tsx
    import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

    export default function ImpactPage() {
      return (
        <div className="space-y-6">
          <div>
            <h1 className="text-2xl font-bold text-gray-900">Impact</h1>
            <p className="text-gray-500 mt-1">Environmental impact and equivalents</p>
          </div>
          <Card>
            <CardHeader>
              <CardTitle>Environmental Impact</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-gray-600">
                CO2 savings, trees equivalent, waste diversion metrics, and tangible comparisons will appear here.
              </p>
            </CardContent>
          </Card>
        </div>
      );
    }
    ```

    **apps/web/app/model/page.tsx**:
    ```tsx
    import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

    export default function ModelPage() {
      return (
        <div className="space-y-6">
          <div>
            <h1 className="text-2xl font-bold text-gray-900">Model</h1>
            <p className="text-gray-500 mt-1">YOLO perception model performance</p>
          </div>
          <Card>
            <CardHeader>
              <CardTitle>Model Performance</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-gray-600">
                Confidence banding, distribution charts, uncertain rate, and fallback analysis will appear here.
              </p>
            </CardContent>
          </Card>
        </div>
      );
    }
    ```

    **apps/web/app/settings/page.tsx**:
    ```tsx
    import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

    export default function SettingsPage() {
      return (
        <div className="space-y-6">
          <div>
            <h1 className="text-2xl font-bold text-gray-900">Settings</h1>
            <p className="text-gray-500 mt-1">Device configuration and system preferences</p>
          </div>
          <Card>
            <CardHeader>
              <CardTitle>Device Settings</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-gray-600">
                Registered device metadata, location, status, and configuration options will appear here.
              </p>
            </CardContent>
          </Card>
        </div>
      );
    }
    ```

    All pages follow the same structural pattern:
    - h1 with text-2xl font-bold for page title
    - p with text-gray-500 for subtitle
    - Card wrapping placeholder content description
    - space-y-6 for consistent vertical spacing
  </action>
  <verify>
    <automated>cd apps/web && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/page.tsx contains "DashboardPage" and "Dashboard" and "Card"
    - apps/web/app/analytics/page.tsx contains "AnalyticsPage" and "Analytics"
    - apps/web/app/impact/page.tsx contains "ImpactPage" and "Impact"
    - apps/web/app/model/page.tsx contains "ModelPage" and "Model"
    - apps/web/app/settings/page.tsx contains "SettingsPage" and "Settings"
    - All 5 page files import from "@/components/ui/card"
    - `cd apps/web && npm run build` exits with code 0 (all routes compile)
  </acceptance_criteria>
  <done>
    All 5 routes render placeholder pages with consistent layout: page title, subtitle, and Card with description of upcoming content. Navigation between all pages works via the sidebar.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && npm run build` completes without errors
2. Navigate to localhost:3000 -- sidebar visible with ECO DASHBOARD branding and 5 nav links
3. Click each nav link -- routes to correct page with placeholder content
4. Active nav item highlighted in sidebar matches current URL
5. Admin user block visible at bottom of sidebar with avatar
6. Top bar shows "Smart Dashboard" title and user avatar
7. On mobile viewport: sidebar hidden, hamburger menu opens Sheet drawer with sidebar
8. Background uses eco-bg color (light blue-ish) behind white cards
</verification>

<success_criteria>
- DASH-01 satisfied: Left sidebar with "ECO DASHBOARD" branding, nav icons for Dashboard/Analytics/Impact/Model/Settings, Admin user at bottom
- VISL-02 satisfied: All visible elements use shadcn/ui components (Card, Button, Avatar, Badge, Tooltip, Separator, Sheet)
- All 5 routes work and render consistent placeholder pages
- Active nav state highlights current page
- Mobile-responsive with Sheet-based sidebar drawer
- Eco-tech visual tone: green accents, white cards, light background
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-shell-and-navigation/01-02-SUMMARY.md`
</output>
